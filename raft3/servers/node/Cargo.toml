[package]
name = "server"
version = "0.1.0"
edition = "2021"

[dependencies]
proto = { path = "../../proto" }
tokio = { version = "1", features = ["full"] }
anyhow = "1"
clap = { version = "4", features = ["derive"] }
